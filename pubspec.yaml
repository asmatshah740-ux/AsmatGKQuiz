name: asmat_gk_quiz
description: Asmat's World - online daily general knowledge quiz app.
publish_to: 'none'
version: 1.0.0+10

environment:
  sdk: '>=3.10.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.17.2
  google_mobile_ads: ^9.1.0
  shared_preferences: ^2.5.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
